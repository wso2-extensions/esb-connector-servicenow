{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Servicenow API Get Aggregate Record Operation Response Schema",
  "description": "Schema representing the output of the get aggregate record operation in ServiceNow API",
  "properties": {
    "payload": {
      "description": "Response for the `getAggregateRecord` method.",
      "type": "object",
      "properties": {
        "result": {
          "description": "The result of the aggregate operation.",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "stats": {
                "description": "Statistics for this aggregate row.",
                "type": "object"
              }
            }
          }
        }
      }
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}
