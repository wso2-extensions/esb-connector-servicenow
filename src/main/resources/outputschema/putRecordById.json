{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Output Schema for putRecordById Operation",
    "description": "Output schema for the putRecordById operation in the ServiceNow connector.",
    "properties": {
        "payload": {
            "type": "object",
            "description": "The main response payload from the putRecordById operation.",
            "properties": {
                "result": {
                    "type": "object",
                    "description": "The replaced record in the ServiceNow table."
                }
            }
        },
        "headers": {
            "type": "object",
            "description": "HTTP headers from the ServiceNow API response.",
            "properties": {
                "Content-Type": {
                    "type": "string",
                    "description": "The content type of the response."
                }
            }
        },
        "attributes": {
            "type": "object",
            "description": "Metadata about the putRecordById operation.",
            "properties": {
                "statusCode": {
                    "type": "integer",
                    "description": "The HTTP status code of the response."
                }
            }
        }
    },
    "required": [
        "payload",
        "attributes"
    ],
    "additionalProperties": false
}