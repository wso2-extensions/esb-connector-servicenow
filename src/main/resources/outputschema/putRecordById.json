{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Servicenow API Add Votes for Put Record By Id Response Schema",
  "description": "Schema representing the output of the put record by id operation in ServiceNow API",
  "properties": {
    "payload": {
      "description": "Response for the `putRecordById` method.",
      "properties": {
        "result": {
          "description": "The result of the aggregate operation.",
          "type": "object"
        }
      }
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}
