{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Output Schema for patchRecordById Operation",
    "description": "Output schema for the patchRecordById operation in the ServiceNow connector.",
    "properties": {
        "payload": {
            "type": "object",
            "description": "The main response payload from the patchRecordById operation.",
            "properties": {
                "result": {
                    "type": "object",
                    "description": "The updated record in the ServiceNow table."
                }
            }
        },
        "headers": {
            "type": "object",
            "description": "HTTP headers from the ServiceNow API response.",
            "properties": {
                "Content-Type": {
                    "type": "string",
                    "description": "The content type of the response."
                }
            }
        },
        "attributes": {
            "type": "object",
            "description": "Metadata about the patchRecordById operation.",
            "properties": {
                "statusCode": {
                    "type": "integer",
                    "description": "The HTTP status code of the response."
                }
            }
        }
    },
    "required": [
        "payload",
        "attributes"
    ],
    "additionalProperties": false
}