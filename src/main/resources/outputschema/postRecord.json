{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Output Schema for postRecord Operation",
    "description": "Output schema for the postRecord operation in the ServiceNow connector.",
    "properties": {
        "payload": {
            "type": "object",
            "description": "The main response payload from the postRecord operation.",
            "properties": {
                "result": {
                    "type": "object",
                    "description": "The created record in the ServiceNow table."
                }
            }
        },
        "headers": {
            "type": "object",
            "description": "HTTP headers from the ServiceNow API response.",
            "properties": {
                "Content-Type": {
                    "type": "string",
                    "description": "The content type of the response."
                },
                "Location": {
                    "type": "string",
                    "description": "The URL of the newly created record."
                }
            }
        },
        "attributes": {
            "type": "object",
            "description": "Metadata about the postRecord operation.",
            "properties": {
                "statusCode": {
                    "type": "integer",
                    "description": "The HTTP status code of the response."
                }
            }
        }
    },
    "required": [
        "payload",
        "attributes"
    ],
    "additionalProperties": false
}